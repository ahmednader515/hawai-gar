/** Defaults when no SiteSetting row exists (must match DB seed expectations). */
export const DEFAULT_SAR_PER_KM = 500;
/** Default kilometers covered by `DEFAULT_SAR_PER_KM`. */
export const DEFAULT_KM_PER_PRICE_UNIT = 1;
/** Default company profit margin percentage added on top of request price. */
export const DEFAULT_COMPANY_PROFIT_MARGIN_PCT = 15;
